{
  "name": "chimera-ai-platform",
  "version": "2.0.0",
  "description": "Chimera AI - Monica Authority 99.999%",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build completed'",
    "test": "cypress run",
    "cypress:open": "cypress open",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0",
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/client-s3": "^3.490.0",
    "aws-sdk": "^2.1530.0"
  },
  "devDependencies": {
    "cypress": "^13.6.2",
    "@netlify/plugin-lighthouse": "^4.0.0",
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
